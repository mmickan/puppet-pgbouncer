[Unit]
Description=PgBouncer
After=network.target

[Service]
User=postgres
ExecStart=/usr/sbin/pgbouncer /etc/pgbouncer/pgbouncer_%i.ini
ExecReload=/usr/sbin/pgbouncer -R /etc/pgbouncer/pgbouncer_%i.ini

[Install]
WantedBy=multi-user.target
